{
    "configurations": [
    {
        "name": "Attach to Edge",
        "url": "http://localhost:2000",
        "request": "launch",
        "type": "msedge",
        "webRoot": "${workspaceFolder}"
    }
    ]
}